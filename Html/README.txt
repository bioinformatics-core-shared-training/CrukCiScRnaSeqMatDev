Development. HTML reports generated by rendering chapters separately in separate sessions, with rmarkdown::render(), which I reverted to when rendering the whole book adding a chapter at a time proved cumbersome due to caching issues.
